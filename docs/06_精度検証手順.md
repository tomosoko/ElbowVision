# 06 精度検証手順（CT真値 vs AI 比較・Bland-Altman 解析）

## 目的

AIによるポジショニングエラー推定値と、**CT由来の真値角度**を比較する。
CT真値をゴールドスタンダードとしてBland-Altman法で一致性を評価し、臨床使用可否を判断する。

> **なぜCT真値か？**
> CTボリュームから実際の骨の3D形状・角度を計算できるため、人間の手動計測より正確。
> `elbow_synth.py` が生成する `dataset_summary.csv` の `rotation_error_deg` がそれに相当する。

---

## Step 1 : CT真値データの収集

### CT真値はすでにある

`elbow_synth.py` でDRR生成時に `data/yolo_dataset/dataset_summary.csv` に自動出力済み。

```bash
cat data/yolo_dataset/dataset_summary.csv | head
# → filename, view_type, rotation_error_deg, flexion_deg, ...
```

| 列 | 意味 |
|---|---|
| `rotation_error_deg` | 理想位からの回旋ズレ量（°）← CT真値 |
| `flexion_deg` | 肘屈曲角（°）← CT真値 |

### 追加の手動計測（任意・補助）

施設のPACS/ビューアで確認したい場合のみ:

| 計測項目 | 計測方法 | 記録単位 |
|---------|---------|---------|
| 外反角（Carrying Angle） | AP像: 上腕骨長軸と前腕骨長軸の角度 | 度（小数点1位） |
| 屈曲角（Flexion） | 側面像: 上腕骨と前腕骨の角度 | 度（小数点1位） |

---

## Step 2 : AI計測値の収集

APIを使って各画像を自動計測し、CSVに出力する。

### 一括計測スクリプト

```bash
cd /Users/kohei/Dev/ElbowVision_Dev
source elbow-api/venv/bin/activate

python elbow-train/batch_analyze.py \
  --input  data/images/val/ \
  --output validation_output/ai_results.csv
```

（`batch_analyze.py` は下記参照）

### 手動でAPIを使って計測する場合

```bash
# 1枚ずつ計測
curl -X POST http://localhost:8000/api/analyze \
  -F "file=@data/images/val/LAT_001.png" \
  | python3 -c "
import json, sys
d = json.load(sys.stdin)
pc = d['positioning_correction']
print(f'rotation_error: {pc[\"rotation_error\"]}°, flexion: {pc[\"flexion_deg\"]}°, level: {pc[\"overall_level\"]}')
"
```

---

## Step 3 : CT真値とAI推定をCSVにまとめる

`dataset_summary.csv`（CT真値）と `ai_results.csv`（AI推定）をマージする:

```csv
image_id,ct_rotation_error,ai_rotation_error,ct_flexion_deg,ai_flexion_deg,view_type
LAT_rot10_001,10.0,9.7,90.0,91.2,LAT
LAT_rot-5_001,-5.0,-4.8,88.0,87.5,LAT
AP_rot3_001,3.0,3.4,170.0,0.0,AP
...
```

---

## Step 4 : Bland-Altman 解析の実行

```bash
cd /Users/kohei/Dev/ElbowVision_Dev
source elbow-api/venv/bin/activate

python bland_altman_analysis.py --csv validation_output/results.csv
```

### 出力ファイル

```
validation_output/
├── bland_altman_rotation_error.png    # 回旋エラー（CT真値 vs AI推定）
├── bland_altman_flexion.png           # 屈曲角（CT真値 vs AI推定）
└── bland_altman_summary.csv          # 平均差・LoA・相関係数
```

---

## Step 5 : 結果の解釈

### 見方

```
グラフの読み方:
  横軸 = CT真値とAI推定の平均値
  縦軸 = AI推定 - CT真値の差
  中央線（緑）= 平均差（バイアス）
  上下の破線（赤）= ± 1.96SD（一致の限界: LoA）

理想的な結果:
  ・平均差がゼロに近い（バイアスが小さい）
  ・LoAが臨床許容範囲内
  ・点が均等に分布している（系統的なズレがない）
```

### 回旋エラーの臨床許容範囲

| 指標 | 目標 | 備考 |
|-----|------|------|
| 平均差（バイアス） | ±1° 以内 | |
| LoA（95%一致限界） | ±5° 以内 | RT基準の「±5°以内=良好」に準拠 |
| 相関係数 | r > 0.95 | |

### 屈曲角の臨床許容範囲

| 指標 | 目標 |
|-----|------|
| 平均差 | ±3° 以内 |
| LoA | ±8° 以内 |

### ポジショニング判定精度（分類）

ポジショニングガイダンスシステムとして「good/minor/major」の分類精度も重要:

| 指標 | 目標 |
|-----|------|
| 3クラス分類精度 | > 85% |
| good/major の混同率 | 0%（見逃し厳禁） |

---

## 一括計測スクリプト（batch_analyze.py）

```python
# elbow-train/batch_analyze.py として保存して使う
"""
val画像を一括でAPI解析してCSVに出力するスクリプト

使い方:
  python elbow-train/batch_analyze.py \
    --input data/images/val/ \
    --output validation_output/ai_results.csv
"""
import argparse, csv, os, sys, requests

def main():
    parser = argparse.ArgumentParser()
    parser.add_argument("--input",  required=True)
    parser.add_argument("--output", required=True)
    parser.add_argument("--api",    default="http://localhost:8000")
    args = parser.parse_args()

    os.makedirs(os.path.dirname(args.output), exist_ok=True)
    images = sorted([f for f in os.listdir(args.input) if f.endswith('.png')])

    with open(args.output, "w", newline="") as f:
        writer = csv.writer(f)
        writer.writerow(["image_id", "ai_carrying", "ai_flexion", "ai_pronation",
                         "ai_varus_valgus", "view_type", "qa_score"])
        for fname in images:
            path = os.path.join(args.input, fname)
            with open(path, "rb") as img:
                r = requests.post(f"{args.api}/api/analyze",
                                  files={"file": (fname, img, "image/png")})
            if r.status_code != 200:
                print(f"SKIP: {fname} ({r.status_code})")
                continue
            d = r.json()
            a = d["landmarks"]["angles"]
            q = d["landmarks"]["qa"]
            writer.writerow([
                os.path.splitext(fname)[0],
                a["carrying_angle"], a["flexion"], a["pronation_sup"],
                a["varus_valgus"], q["view_type"], q["score"]
            ])
            print(f"OK: {fname}  carrying={a['carrying_angle']}  flexion={a['flexion']}")

if __name__ == "__main__":
    main()
```

---

## 検証報告書の作成

結果をまとめて上司・発表資料に使う。
`validation_output/` 以下の画像をスライドに貼り付けて説明する。

報告書に含める内容:
1. 対象画像数（n=XX）
2. 各角度のBland-Altmanプロット
3. 平均差・LoA・相関係数の表
4. 臨床許容範囲との比較
5. 考察（バイアスが大きい場合の原因と対策）
